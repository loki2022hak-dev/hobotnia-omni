generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/test"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model TestModel {
  id String @id
}
